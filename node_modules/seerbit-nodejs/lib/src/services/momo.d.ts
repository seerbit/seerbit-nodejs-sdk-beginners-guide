import Client from "../client";
import Service from "../service";
declare class Momo extends Service {
    private readonly _momoPayment;
    private readonly _momoNetworks;
    constructor(client: Client);
    InitializeTransaction(requestPayload: IMobileMoney.InitializeRequest): Promise<IMobileMoney.InitializeResponse>;
    MobileMoneyNetworks(requestPayload?: IMobileMoney.GetNetworksRequest): Promise<IMobileMoney.GetNetworksResponse>;
}
export default Momo;
