import Client from "../client";
import Service from "../service";
declare class Recurrent extends Service {
    private readonly _bankList;
    constructor(client: Client);
    BanksList(requestPayload?: IAccount.BankListRequest): Promise<IAccount.BankListResponse>;
}
export default Recurrent;
