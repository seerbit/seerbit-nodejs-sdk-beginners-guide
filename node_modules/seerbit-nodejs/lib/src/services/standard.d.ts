import Client from "../client";
import Service from "../service";
declare class Standard extends Service {
    private readonly _initializeStandardTransaction;
    constructor(client: Client);
    Initialize(requestPayload: IStandard.InitializeRequest): Promise<IStandard.InitializeResponse>;
    private ensurePayloadIsComplete;
}
export default Standard;
