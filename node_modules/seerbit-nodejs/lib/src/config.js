"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Config {
    constructor(configuration) {
        if (configuration.environment)
            this.environment = configuration.environment || "LIVE";
        this.secretKey = configuration.secretKey;
        this.publicKey = configuration.publicKey;
        this.bearerToken = configuration.bearerToken;
        if (configuration.endpoint)
            this.endpoint = configuration.endpoint;
        if (configuration.connectionTimeoutMillis)
            this.connectionTimeoutMillis = configuration.connectionTimeoutMillis || 30000;
        if (configuration.readTimeoutMillis)
            this.readTimeoutMillis = configuration.readTimeoutMillis || 3000;
        if (configuration.httpClient)
            this.httpClient = configuration.httpClient;
    }
}
exports.default = Config;
//# sourceMappingURL=config.js.map