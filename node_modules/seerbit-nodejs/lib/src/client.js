"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const httpURLConnectionClient_1 = __importDefault(require("./httpClient/httpURLConnectionClient"));
// interface ClientParameters {
//     config?: Config;
//     environment?: Environment;
//     publicKey: string;
//     secretKey: string;
//     httpClient?: ClientInterface;
// }
class Client {
    constructor(config) {
        this.config = config;
        const environment = config.environment;
        if (environment) {
            this.setEnvironment(environment);
        }
        else {
            this.setEnvironment("LIVE");
        }
        if (config.httpClient) {
            this._httpClient = config.httpClient;
        }
    }
    setEnvironment(environment) {
        if (environment === "PILOT") {
            this.config.endpoint = Client.ENDPOINT_PILOT;
        }
        else if (environment === "LIVE") {
            this.config.endpoint = Client.ENDPOINT_LIVE;
        }
        else {
            this.config.endpoint = Client.ENDPOINT_LIVE;
        }
    }
    get httpClient() {
        if (!this._httpClient) {
            this._httpClient = new httpURLConnectionClient_1.default();
        }
        return this._httpClient;
    }
    set httpClient(httpClient) {
        this._httpClient = httpClient;
    }
    setTimeouts(connectionTimeoutMillis, readTimeoutMillis) {
        this.config.connectionTimeoutMillis = connectionTimeoutMillis;
        this.config.readTimeoutMillis = readTimeoutMillis;
    }
}
//DEFINE BASE URLS
Client.ENDPOINT_PILOT = "https://pilot-backend.seerbitapi.com/v2/";
Client.ENDPOINT_LIVE = "https://seerbitapi.com/api/v2/";
exports.default = Client;
//# sourceMappingURL=client.js.map